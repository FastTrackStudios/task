apiVersion: v2
name: task
description: >-
  Self-hosted Task — local-first project/issue tracker. Deploys the
  task-server (vox websocket API + attachments + per-org data) and the
  static wasm web app, with optional ui-lab component gallery.
type: application
home: https://codeberg.org/FastTrackStudios/Task
sources:
  - https://codeberg.org/FastTrackStudios/Task
# Chart version — bump on chart changes.
version: 0.1.0
# App version tracks the repo's workspace version (Cargo.toml
# [workspace.package].version). Image tags default to this unless
# overridden per-image in values.
appVersion: "0.1.0"
