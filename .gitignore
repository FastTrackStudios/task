/target
**/target
/dist
**/dist
.direnv/
result
result-*
*.profraw
.DS_Store

# Generated by `just --justfile apps/Justfile --working-directory apps css`.
# The INPUTS (apps/*/tailwind.css) are committed; these outputs are not.
# They must exist before cargo runs, because asset!() resolves them at
# compile time — dx builds them during `dx serve`/`dx build`, and CI
# generates them explicitly (see .github/workflows/checks.yml).
apps/web/assets/tailwind.css
apps/desktop/assets/tailwind.css
apps/mobile/assets/tailwind.css

# npm install --no-save tailwindcss in CI drops this here.
/node_modules
