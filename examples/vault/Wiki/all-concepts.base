filters:
  and:
    - { property: type, equals: concept }

views:
  - name: All concepts
    type: table
    columns: [title, tags, status]
  - name: Stable only
    type: table
    filter:
      and:
        - { property: status, equals: stable }
    columns: [title, tags]
